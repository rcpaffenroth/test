[project]
name = "test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest>=9.0.2",
    "nbmake>=1.5.5",
]

[tool.pytest.ini_options]
testpaths = ["tests/"]
addopts = "--nbmake"
